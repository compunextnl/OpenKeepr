# =============================================================================
# OpenKeepr — Python dependencies (single combined list)
#
# Production deployments can safely install all of these; the few dev / test
# tools at the bottom add ~10 MB and have no runtime effect when not invoked.
# =============================================================================

# --- Core ---
Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.4
gunicorn==22.0.0

# --- Config ---
python-dotenv==1.0.1

# --- Database / migrations ---
SQLAlchemy==2.0.30
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.1

# --- Forms / CSRF ---
Flask-WTF==1.2.1
WTForms==3.1.2
email-validator==2.1.1

# --- Auth / 2FA ---
Flask-Login==0.6.3
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode[pil]==7.4.2

# --- i18n ---
Flask-Babel==4.0.0
Babel==2.15.0

# --- Crypto / security ---
cryptography==42.0.7
itsdangerous==2.2.0

# --- Rate limiting ---
Flask-Limiter==3.7.0

# --- Markdown rendering (release notes, API docs, decrypted MD) ---
markdown==3.6
Pygments==2.18.0
bleach==6.1.0

# --- Scheduler (background cleanup) ---
APScheduler==3.10.4

# --- HTTP (fetch_assets and provider APIs) ---
requests==2.32.2

# --- Dev / test ---
# Installed alongside the runtime dependencies for simplicity. Pin to the
# same versions you use locally so CI is deterministic.
pytest==8.2.1
pytest-flask==1.3.0
pytest-cov==5.0.0
ruff==0.4.7
mypy==1.10.0
types-requests==2.32.0.20240602
